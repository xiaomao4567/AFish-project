<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yitiaoyu.server.mapper.CartMapper">

    <resultMap id="CartResultMap" type="com.example.yitiaoyu.pojo.entity.Cart">
        <id column="id" property="id"/>
        <result column="table_number" property="tableNumber"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="CartResultMap">
        SELECT * FROM cart WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.yitiaoyu.pojo.entity.Cart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (table_number, create_time, update_time) 
        VALUES (#{tableNumber}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="com.example.yitiaoyu.pojo.entity.Cart">
        UPDATE cart 
        SET table_number = #{tableNumber}, update_time = #{updateTime} 
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <select id="selectByTableNumber" parameterType="Integer" resultMap="CartResultMap">
        SELECT * FROM cart WHERE table_number = #{tableNumber}
    </select>

</mapper>
