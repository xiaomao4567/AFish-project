<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yitiaoyu.server.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.example.yitiaoyu.pojo.entity.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.yitiaoyu.pojo.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, sort_order, create_time, update_time) 
        VALUES (#{name}, #{sortOrder}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="com.example.yitiaoyu.pojo.entity.Category">
        UPDATE category 
        SET name = #{name}, sort_order = #{sortOrder}, update_time = #{updateTime} 
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="selectAll" resultMap="CategoryResultMap">
        SELECT * FROM category ORDER BY sort_order ASC
    </select>

    <select id="selectCount" resultType="Integer">
        SELECT COUNT(*) FROM category
    </select>

</mapper>
