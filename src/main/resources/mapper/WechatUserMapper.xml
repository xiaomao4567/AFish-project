<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yitiaoyu.server.mapper.WechatUserMapper">

    <select id="selectByOpenId" resultType="com.example.yitiaoyu.pojo.entity.WechatUser">
        SELECT id, open_id, nickname, avatar_url, created_at, updated_at
        FROM wechat_user
        WHERE open_id = #{openId}
    </select>

    <insert id="insert" parameterType="com.example.yitiaoyu.pojo.entity.WechatUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wechat_user (open_id, nickname, avatar_url, created_at, updated_at)
        VALUES (#{openId}, #{nickname}, #{avatarUrl}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateByOpenId" parameterType="com.example.yitiaoyu.pojo.entity.WechatUser">
        UPDATE wechat_user
        SET nickname = #{nickname}, avatar_url = #{avatarUrl}, updated_at = #{updatedAt}
        WHERE open_id = #{openId}
    </update>

</mapper>