<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yitiaoyu.server.mapper.OrderItemMapper">

    <resultMap id="OrderItemResultMap" type="com.example.yitiaoyu.pojo.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="dish_id" property="dishId"/>
        <result column="dish_name" property="dishName"/>
        <result column="image" property="image"/>
        <result column="flavor" property="flavor"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="OrderItemResultMap">
        SELECT * FROM order_item WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.yitiaoyu.pojo.entity.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, dish_id, dish_name, image, flavor, quantity, price, create_time) 
        VALUES (#{orderId}, #{dishId}, #{dishName}, #{image}, #{flavor}, #{quantity}, #{price}, #{createTime})
    </insert>

    <update id="updateById" parameterType="com.example.yitiaoyu.pojo.entity.OrderItem">
        UPDATE order_item 
        SET order_id = #{orderId}, dish_id = #{dishId}, dish_name = #{dishName}, 
            image = #{image}, flavor = #{flavor}, quantity = #{quantity}, price = #{price} 
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM order_item WHERE id = #{id}
    </delete>

    <select id="selectByOrderId" parameterType="Long" resultMap="OrderItemResultMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <delete id="deleteByOrderId" parameterType="Long">
        DELETE FROM order_item WHERE order_id = #{orderId}
    </delete>

</mapper>
